#!/bin/bash

V_MINOR=${V_MINOR:-1}
V_PATCH=${V_PATCH:-2}
V_FUNCS=${V_FUNCS:-"version-file"}

REMOTE=${REMOTE:-"origin"}
TAG_PREFIX=${TAG_PREFIX:-"v"}

GIT_USER_NAME=${GIT_USER_NAME:-"github-actions"}
GIT_USER_EMAIL=${GIT_USER_EMAIL:-"github-actions@github.com"}
