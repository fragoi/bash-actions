#!/bin/bash

## Handles version using 'debian/changelog' file.

V_FILE='debian/changelog'

getVersion() {
  debChangelogVersion
}

setVersion() {
  local version=$1
  debChangelogVersionNew "$version"
}
